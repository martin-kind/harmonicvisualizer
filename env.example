# Copy this to `.env.local` for local development (Next.js auto-loads it).
# Never commit your real keys.

# Required: used by `app/api/chord/route.ts`
OPENAI_API_KEY=

# Optional: defaults to "gpt-4o-mini"
OPENAI_MODEL=


